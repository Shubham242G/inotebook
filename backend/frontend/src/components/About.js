import React,{useContext, useEffect} from 'react'
import noteContext from './context/notes/noteContext'

const About = () =>{
    return(
        <div>
            This application is made to register any important notes that the user
            wants to remember at ay future time.
        </div>
    )
}
export default About